{
  "mcpServers": {
    "clockify-agent-plugin": {
      "command": "node",
      "args": ["dist/index.js"],
      "env": {
        "CLOCKIFY_API_KEY": "${CLOCKIFY_API_KEY}",
        "CLOCKIFY_WORKSPACE_ID": "${CLOCKIFY_WORKSPACE_ID}"
      }
    }
  }
}
