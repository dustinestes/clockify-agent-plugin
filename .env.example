# Maintainer Sandbox
# Not read by the MCP server. --sandbox bakes this into sandbox mcp.json env.
# Do not put consumer CLOCKIFY_API_KEY here — that is for MCP/plugin hosts.
CLOCKIFY_API_KEY_SANDBOX=
